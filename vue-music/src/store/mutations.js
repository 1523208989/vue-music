export default {
  setSinger(state, item) {
    state.singer = item
  },
  setPlayList(state, item) {
    state.playList = item
  },
  setFullScroll(state, item) {
    state.fullScroll = item
  },
  setSong(state, item) {
    state.song = item
  },
  setIndex(state, item) {
    state.index = item
  },
  setMode(state, item) {
    state.mode = item
  }
}